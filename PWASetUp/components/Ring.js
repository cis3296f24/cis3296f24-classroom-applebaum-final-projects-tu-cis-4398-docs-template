import React from 'react'
import { Button } from '@nextui-org/react'

const RingDevice = () => {
    return (
        <div>
            <Button color="primary">Ring Device</Button>
        </div>
    );
};

export default RingDevice